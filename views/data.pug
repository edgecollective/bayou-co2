doctype html
html
    head
        meta(charset='UTF-8')
        title Bayou
        link(rel='stylesheet', href='/style.css')

<a href="/">Back to Bayou Home</a>

<hr>

h1 Home Page for Bayou-CO2 Feed: "#{feed_name}"

<pre><code>
<b>Name:</b> "#{feed_name}"<br>
<b>Public Key:</b> #{feed_pubkey} <br>
<b>Private Key:</b> [Secret]<br>
<b>Home Page:</b> <a href="/data/#{feed_pubkey}">http://#{base_url}/data/#{feed_pubkey}</a>
</pre></code>

h1 Downloading Data:

p <b>All feed data</b>: <a href="/data/#{feed_pubkey}/csv/">CSV</a> | <a href="/data/#{feed_pubkey}/json/">JSON</a>

p <b>Most recent upload only</b>: <a href="/data/#{feed_pubkey}/json/">JSON</a>
