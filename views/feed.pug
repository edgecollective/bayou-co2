doctype html
html
    head
        meta(charset='UTF-8')
        title Bayou
        link(rel='stylesheet', href='/style.css')

h1 You've created a new data feed!

<pre><code>
 <b>Name:</b> "#{feed_name}"<br>
<b>Public Key:</b> #{feed_pubkey} <br>
<b>Private Key:</b> #{private_key} <br>
<b>Feed Home Page:</b> <a href="/data/#{feed_pubkey}">http://#{base_url}/data/#{feed_pubkey}</a>
</pre></code>

p.blurb (<b>IMPORTANT</b>: you should immediately record the above information -- these keys won't be shown again!)

<hr>

<a href="/">Back to Bayou Home</a>
